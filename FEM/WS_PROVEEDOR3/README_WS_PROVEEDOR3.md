# WS_PROVEEDOR3

Esta historia está implementada como operación dentro del Web Service principal del proveedor:

`FEM\WS_PROVEEDOR1\WS_PROVEEDOR.sln`

Método SOAP:

`CalcularFacturacion`

Esto se dejó así porque el PDF indica que WS_PROVEEDOR3 agrega una operación al Web Service del proveedor, no que obligatoriamente sea un proyecto separado.
